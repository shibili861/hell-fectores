
 <%- include('../partials/user/header') %> 


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ÉLÉGANCE - Luxury Men's Fashion</title>
  
  
  
  <style>  
   @import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600&family=Montserrat:wght@300;400;500;600;700&display=swap');
 
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html { scroll-behavior: smooth; }

body {
  font-family: 'Montserrat', sans-serif;
  background: #ffffff;
  color: #1a1a1a;
  line-height: 1.6;
}

/* ================= ROOT ================= */
:root {
  --primary: #8b7355;
  --primary-dark: #6b4423;
  --text-dark: #1a1a1a;
  --text-muted: #777;
  --white: #ffffff;

  --shadow-soft: 0 10px 30px rgba(0,0,0,0.08);
  --shadow-hover: 0 25px 60px rgba(0,0,0,0.15);

  --radius: 18px;
  --transition: all 0.4s ease;
}
/* ================= HERO SECTION ================= */
/* =====================================================
   HERO SECTION — COMPLETE & FIXED
===================================================== */

.hero {
  position: relative;
  height: 90vh;
  min-height: 720px;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;

  background:
    linear-gradient(
      rgba(255, 255, 255, 0.86),
      rgba(255, 255, 255, 0.94)
    ),
    url("https://images.unsplash.com/photo-1520975916090-3105956dac38?auto=format&fit=crop&w=2000&q=80")
    center / cover no-repeat;

  overflow: hidden;
}

/* IMPORTANT: allow hover/click */
.hero::before,
.hero::after {
  pointer-events: none;
}

.hero-content {
  position: relative;
  z-index: 5;
  max-width: 980px;
  padding: 0 20px;
}

/* =====================================================
   TOP LABEL
===================================================== */

.hero-subtitle-top {
  display: inline-block;
  font-size: 9px;
  letter-spacing: 6px;
  font-weight: 600;
  text-transform: uppercase;
  color: #5f7f89;
  margin-bottom: 26px;
  opacity: 0;
  animation: fadeUp 0.8s ease forwards;
}

.hero-subtitle-top::after {
  content: '';
  display: block;
  width: 46px;
  height: 1px;
  margin: 18px auto 0;
  background: linear-gradient(135deg, #003543, #005566);
}

/* =====================================================
   MAIN TITLE
===================================================== */

.hero h1 {
  font-family: 'Cormorant Garamond', serif;
  font-size: 92px;
  letter-spacing: 12px;
  font-weight: 500;
  text-transform: uppercase;
  line-height: 1.05;
  margin-bottom: 36px;

  background: linear-gradient(
    135deg,
    #003543,
    #005566,
    #1a6f7c
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;

  opacity: 0;
  animation: fadeUp 1.1s ease forwards;
}

.hero h1::after {
  content: '';
  display: block;
  width: 110px;
  height: 1px;
  margin: 32px auto 0;
  background: linear-gradient(135deg, #003543, #005566);
}

/* =====================================================
   SUBTITLE
===================================================== */

.hero-subtitle {
  font-size: 14px;
  letter-spacing: 2.5px;
  color: #6b7f86;
  max-width: 600px;
  margin: 0 auto 56px;
  line-height: 1.9;
  font-weight: 400;

  opacity: 0;
  animation: fadeUp 1.4s ease forwards;
}

/* =====================================================
   BUTTONS
===================================================== */
.hero-buttons {
  display: flex;
  justify-content: center;
  gap: 22px;

  opacity: 1;
  transform: translateY(34px);
  animation: heroButtonsIn 1.7s ease forwards;
}
@keyframes heroButtonsIn {
  to {
    transform: translateY(0);
  }
}

.cta-primary,
.cta-secondary {
  position: relative;
  z-index: 10;
  pointer-events: auto;
}


/* Primary Button */
.cta-primary {
  padding: 16px 52px;
  font-size: 10px;
  letter-spacing: 3.5px;
  font-weight: 700;
  text-transform: uppercase;
  border-radius: 32px;

  background: linear-gradient(135deg, #003543, #005566);
  color: #ffffff;

  border: none;
  position: relative;
  overflow: hidden;

  transition: transform 0.35s ease, box-shadow 0.35s ease;
  box-shadow: 0 16px 40px rgba(0,53,67,0.35);
}

/* Shine effect */
.cta-primary::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(
    120deg,
    transparent,
    rgba(255,255,255,0.35),
    transparent
  );
  transform: translateX(-120%);
  transition: transform 0.6s ease;
}

.cta-primary:hover::before {
  transform: translateX(120%);
}

.cta-primary:hover {
  transform: translateY(-6px);
  box-shadow: 0 26px 60px rgba(0,53,67,0.45);
}

/* Secondary Button */
.cta-secondary {
  padding: 16px 52px;
  font-size: 10px;
  letter-spacing: 3.5px;
  font-weight: 600;
  text-transform: uppercase;
  border-radius: 32px;

  background: transparent;
  border: 1px solid rgba(0,53,67,0.45);
  color: #0f2f36;

  transition: all 0.35s ease;
}

.cta-secondary:hover {
  background: rgba(0,53,67,0.08);
  transform: translateY(-4px);
}

/* =====================================================
   ANIMATION
===================================================== */

@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(34px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* =====================================================
   RESPONSIVE
===================================================== */

@media (max-width: 1024px) {
  .hero h1 {
    font-size: 68px;
    letter-spacing: 9px;
  }
}

@media (max-width: 768px) {
  .hero {
    height: 85vh;
  }

  .hero h1 {
    font-size: 44px;
    letter-spacing: 6px;
  }

  .hero-subtitle {
    font-size: 13px;
  }

  .hero-buttons {
    flex-direction: column;
    gap: 16px;
  }

  .cta-primary,
  .cta-secondary {
    width: 100%;
    max-width: 280px;
    margin: 0 auto;
  }
}

/* ================= SECTIONS ================= */
.collections,
.products,
.testimonials {
  padding: 90px 60px;
}

.section-header {
  text-align: center;
  margin-bottom: 60px;
}

.section-label {
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--primary);
  font-weight: 700;
}

.section-title {
  font-size: 48px;
  letter-spacing: 6px;
}


/* ================= SECTIONS ================= */
.collections,
.testimonials {
  background: var(--soft-white);
  padding: 140px 80px;
}

.products,
.featured-banner {
  background: var(--off-white);
  padding: 140px 80px;
}

/* ================= SECTION HEADERS ================= */
.section-header {
  text-align: center;
  margin-bottom: 100px;
}

.section-label {
  font-size: 12px;
  letter-spacing: 4px;
  color: var(--primary);
  font-weight: 700;
}

.section-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 72px;
  font-weight: 400;
  letter-spacing: 8px;
  color: var(--text-dark);
}
/* ================= COLLECTIONS SECTION ================= */
/* ================= COLLECTIONS (ULTRA COMPACT PREMIUM) ================= */

.collections {
  background: 
    linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.92) 0%,
      rgba(250, 250, 250, 0.96) 100%
    ),
    url("https://images.unsplash.com/photo-1520975916090-3105956dac38?auto=format&fit=crop&w=2000&q=80")
    center / cover no-repeat;

  padding: 120px 80px;
}


/* Header */
.section-header {
  text-align: center;
  margin-bottom: 75px;
}

.section-label {
  font-size: 11px;
  letter-spacing: 4px;
  color: var(--primary);
  font-weight: 700;
}

.section-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 60px;
  font-weight: 400;
  letter-spacing: 8px;
  color: var(--text-dark);
}

/* Grid */
.collection-grid {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 36px;
}

/* Card */
.collection-card {
  position: relative;
  height: 460px;
  border-radius: 22px;
  overflow: hidden;
  background: var(--pure-white);

  box-shadow:
    0 18px 40px rgba(0, 53, 67, 0.1),
    0 6px 18px rgba(0, 53, 67, 0.08);

  transition: all 0.55s ease;
}

.collection-card:hover {
  transform: translateY(-10px);
  box-shadow:
    0 30px 60px rgba(0, 53, 67, 0.16),
    0 12px 30px rgba(0, 53, 67, 0.12);
}

/* Background */
.collection-bg {
  position: absolute;
  inset: 0;
  background-size: cover;
  background-position: center;
  transition: transform 1s ease;
}

.collection-card:hover .collection-bg {
  transform: scale(1.08);
}

/* Overlay */
.collection-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to top,
    rgba(0, 0, 0, 0.55) 0%,
    rgba(0, 0, 0, 0.22) 45%,
    rgba(255, 255, 255, 0.06) 100%
  );
}

/* Content */
.collection-content {
  position: absolute;
  bottom: 40px;
  left: 40px;
  right: 40px;
  z-index: 2;
}

.collection-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-size: 40px;
  letter-spacing: 5px;
  margin-bottom: 12px;
  color: #ffffff;
  transition: transform 0.35s ease;
}

.collection-card:hover h3 {
  transform: translateY(-4px);
}

.collection-content p {
  font-size: 12px;
  letter-spacing: 3px;
  color: rgba(255, 255, 255, 0.85);
  margin-bottom: 22px;
  font-weight: 500;
}

/* CTA Button */
.collection-link {
  display: inline-flex;
  align-items: center;
  gap: 10px;

  padding: 12px 28px;
  border-radius: 28px;

  background: rgba(255, 255, 255, 0.95);
  color: var(--primary);

  font-size: 10px;
  letter-spacing: 2.2px;
  font-weight: 800;
  text-transform: uppercase;
  text-decoration: none;

  opacity: 0;
  transform: translateY(18px);

  transition: all 0.4s ease;
  box-shadow: 0 10px 26px rgba(0, 0, 0, 0.28);
}

.collection-link::after {
  content: '→';
  font-size: 16px;
  transition: transform 0.3s ease;
}

.collection-card:hover .collection-link {
  opacity: 1;
  transform: translateY(0);
}

.collection-link:hover {
  background: linear-gradient(135deg, #003543, #005566);
  color: #ffffff;
  box-shadow: 0 16px 40px rgba(0, 53, 67, 0.4);
}

.collection-link:hover::after {
  transform: translateX(5px);
}

/* ================= MOBILE ================= */
@media (max-width: 768px) {
  .collections {
    padding: 80px 28px;
  }

  .collection-grid {
    grid-template-columns: 1fr;
  }

  .collection-card {
    height: 360px;
  }

  .collection-content {
    left: 26px;
    right: 26px;
    bottom: 30px;
  }

  .collection-content h3 {
    font-size: 32px;
  }

  .collection-link {
    opacity: 1;
    transform: translateY(0);
  }
}


/* ================= COLLECTION BUTTON ================= */

.collection-link {
  display: inline-flex;
  align-items: center;
  gap: 12px;

  padding: 14px 36px;
  border-radius: 30px;

  background: rgba(255, 255, 255, 0.92);
  color: var(--primary);

  font-size: 11px;
  letter-spacing: 2.5px;
  font-weight: 700;
  text-transform: uppercase;
  text-decoration: none;

  border: 1px solid rgba(0, 53, 67, 0.25);

  opacity: 0;
  transform: translateY(25px);

  transition: all 0.45s ease;
  box-shadow: 0 10px 28px rgba(0, 53, 67, 0.18);
}

/* Arrow */
.collection-link::after {
  content: '→';
  font-size: 18px;
  transition: transform 0.35s ease;
}

/* Reveal on hover */
.collection-card:hover .collection-link {
  opacity: 1;
  transform: translateY(0);
}

/* Hover effect */
.collection-link:hover {
  background: linear-gradient(135deg, #003543, #005566);
  color: #ffffff;
  border-color: transparent;
  box-shadow: 0 18px 40px rgba(0, 53, 67, 0.35);
}

.collection-link:hover::after {
  transform: translateX(6px);
}

/* ================= MOBILE FIX ================= */
@media (max-width: 768px) {
  .collections {
    padding: 90px 30px;
  }

  .collection-grid {
    grid-template-columns: 1fr;
  }

  .collection-content {
    left: 30px;
    right: 30px;
    bottom: 40px;
  }

  .collection-link {
    opacity: 1;
    transform: translateY(0);
  }

  .collection-content h3 {
    font-size: 40px;
  }
}

/* ================= PRODUCTS ================= */
.product-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 35px;
}

.product-card {
  background: var(--pure-white);
  border-radius: 20px;
  box-shadow:
    0 18px 40px rgba(0, 53, 67, 0.08),
    0 6px 18px rgba(0, 53, 67, 0.06);
  transition: all 0.45s ease;
  position: relative;
}

.product-card:hover {
  transform: translateY(-14px);
  box-shadow:
    0 35px 70px rgba(0, 53, 67, 0.18),
    0 12px 30px rgba(0, 53, 67, 0.12);
}

.product-image {
  height: 420px;
  background: linear-gradient(135deg, #003543, #005566);
}

.product-info {
  padding: 28px;
  text-align: center;
}

.product-category {
  font-size: 10px;
  letter-spacing: 2px;
  color: var(--text-muted);
  font-weight: 600;
}

.product-name {
  font-size: 17px;
  letter-spacing: 2px;
  margin: 12px 0;
  color: var(--text-dark);
}

.product-price {
  font-size: 22px;
  font-weight: 700;
  color: var(--primary);
}

/* ================= CARD ACTION BUTTON ================= */
.card-action {
  position: absolute;
  bottom: 40px;
  left: 50%;
  transform: translateX(-50%) translateY(20px);
  opacity: 0;
  padding: 14px 38px;
  background: linear-gradient(135deg, #003543, #005566);
  color: #ffffff;
  font-size: 11px;
  letter-spacing: 2.5px;
  border-radius: 30px;
  font-weight: 700;
  transition: all 0.4s ease;
  box-shadow: 0 12px 30px rgba(0, 53, 67, 0.35);
}

.collection-card:hover .card-action,
.product-card:hover .card-action {
  opacity: 1;
  transform: translateX(-50%) translateY(0);
}

/* ================= TESTIMONIALS ================= */
.testimonial-text {
  font-family: 'Cormorant Garamond', serif;
  font-size: 32px;
  color: var(--text-dark);
}

.testimonial-author {
  letter-spacing: 3px;
  color: var(--primary);
  font-weight: 700;
}

/* ================= PRODUCT QUICK VIEW FIX ================= */
.product-quick-view {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  padding: 24px;
  display: flex;
  justify-content: center;
  gap: 18px;

  background: linear-gradient(
    to top,
    rgba(0, 0, 0, 0.65),
    rgba(0, 0, 0, 0.25)
  );

  opacity: 0;
  transform: translateY(20px);
  transition: all 0.4s ease;
}

.product-card:hover .product-quick-view {
  opacity: 1;
  transform: translateY(0);
}

/* ================= QUICK BUTTON ================= */
.quick-btn {
  width: 52px;
  height: 52px;
  border-radius: 50%;

  background: rgba(255, 255, 255, 0.9);
  border: none;

  font-size: 18px;
  cursor: pointer;

  display: flex;
  align-items: center;
  justify-content: center;

  transition: all 0.35s ease;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.25);
}

.quick-btn:hover {
  background: linear-gradient(135deg, #003543, #005566);
  color: #ffffff;
  transform: translateY(-6px);
  box-shadow: 0 14px 30px rgba(0, 53, 67, 0.4);
}
.product-badge {
  position: absolute;
  top: 18px;
  left: 18px;

  padding: 8px 18px;
  background: rgba(255, 255, 255, 0.92);
  color: var(--primary);

  font-size: 10px;
  letter-spacing: 2px;
  font-weight: 700;
  border-radius: 20px;

  box-shadow: 0 6px 16px rgba(0, 0, 0, 0.15);
}
.scroll-indicator span {
  display: block;
  width: 2px;
  height: 50px;
  margin: 0 auto 10px;
  background: linear-gradient(to bottom, transparent, var(--primary));
}
.product-card,
.collection-card {
  will-change: transform;
}

/* ======================================================
   COLLECTION GRID — FIXED (3 PER ROW)
====================================================== */
.collection-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 26px;
}

/* Collection Card */
.collection-card {
  height: 340px;
  border-radius: 18px;
  overflow: hidden;
  background: var(--pure-white);
  box-shadow: var(--shadow-soft);
  transition: all 0.45s ease;
  position: relative;
}

.collection-card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-hover);
}

/* Background Image */
.collection-bg {
  position: absolute;
  inset: 0;
  background-size: cover;
  background-position: center;
  transition: transform 0.8s ease;
}

.collection-card:hover .collection-bg {
  transform: scale(1.06);
}

/* Overlay */
.collection-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to top,
    rgba(0,0,0,0.55),
    rgba(0,0,0,0.25),
    transparent
  );
}

/* Content */
.collection-content {
  position: absolute;
  bottom: 26px;
  left: 26px;
  right: 26px;
  z-index: 2;
}

.collection-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-size: 26px;
  letter-spacing: 3px;
  color: #fff;
  margin-bottom: 10px;
}

.collection-content p {
  font-size: 10px;
  letter-spacing: 2px;
  color: rgba(255,255,255,0.85);
  margin-bottom: 14px;
}

/* Button */
.collection-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 9px 22px;
  font-size: 9px;
  letter-spacing: 2px;
  border-radius: 24px;
  background: rgba(255,255,255,0.92);
  color: var(--primary);
  text-decoration: none;
  font-weight: 700;
  opacity: 0;
  transform: translateY(14px);
  transition: all 0.35s ease;
}

.collection-card:hover .collection-link {
  opacity: 1;
  transform: translateY(0);
}

/* ======================================================
   PRODUCTS (SMALLER)
====================================================== */
.product-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 26px;
}

.product-card {
  border-radius: 18px;
  background: var(--pure-white);
  box-shadow: var(--shadow-soft);
  transition: all 0.4s ease;
}

.product-card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-hover);
}

.product-image {
  height: 300px;
}

.product-info {
  padding: 18px;
  text-align: center;
}

.product-category {
  font-size: 9px;
  letter-spacing: 2px;
  color: var(--text-muted);
}

.product-name {
  font-size: 14px;
  margin: 8px 0;
}

.product-price {
  font-size: 16px;
  font-weight: 700;
  color: var(--primary);
}

/* ======================================================
   RESPONSIVE
====================================================== */
@media (max-width: 1200px) {
  .collection-grid {
    grid-template-columns: repeat(2, 1fr);
  }
  .product-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 768px) {
  html { font-size: 14px; }

  .hero h1 {
    font-size: 42px;
    letter-spacing: 4px;
  }

  .collections,
  .products,
  .featured-banner,
  .testimonials {
    padding: 70px 26px;
  }

  .collection-grid,
  .product-grid {
    grid-template-columns: 1fr;
  }

  .collection-card {
    height: 300px;
  }
}


/* ================= RESPONSIVE ================= */
@media (max-width: 1200px) {
  .hero h1 { font-size: 80px; }
  .product-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 768px) {
  .hero h1 { font-size: 48px; letter-spacing: 5px; }
  .collection-grid,
  .product-grid { grid-template-columns: 1fr; }
}

    </style>
</head>
 <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<body>
   

    <section class="hero">
  <div class="hero-content">

    <span class="hero-subtitle-top">Premium Menswear</span>

    <h1>
      Modern<br>
      Elegance
    </h1>

    <p class="hero-subtitle">
      Timeless men’s fashion crafted with precision,
      refined materials, and contemporary design.
    </p>

    <div class="hero-buttons">
      <button class="cta-primary">Shop Collection</button>
      <button class="cta-secondary">Explore More</button>
    </div>

  </div>
</section>




    <!-- Collections -->
    <section class="collections" id="collections">
        <div class="section-header">
            <p class="section-label">EXPLORE</p>
            <h2 class="section-title">Our Collections</h2>
        </div>
        <div class="collection-grid">
            <div class="collection-card">
                <div class="collection-bg" style="background: linear-gradient(135deg, rgba(107, 68, 35, 0.9), rgba(74, 50, 25, 0.8)), url('data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><rect fill=%22%236B4423%22 width=%22100%22 height=%22100%22/></svg>');"></div>
                <div class="collection-overlay"></div>
                <div class="collection-content">
                    <h3>Linen Shirts</h3>
                    <p>BREATHABLE ELEGANCE</p>
                    <a href="#" class="collection-link">Explore Now</a>
                </div>
            </div>
            <div class="collection-card">
                <div class="collection-bg" style="background: linear-gradient(135deg, rgba(139, 115, 85, 0.9), rgba(107, 89, 67, 0.8)), url('data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><rect fill=%22%238B7355%22 width=%22100%22 height=%22100%22/></svg>');"></div>
                <div class="collection-overlay"></div>
                <div class="collection-content">
                    <h3>Linen Pants</h3>
                    <p>EFFORTLESS STYLE</p>
                    <a href="#" class="collection-link">Explore Now</a>
                </div>
            </div>
            <div class="collection-card">
                <div class="collection-bg" style="background: linear-gradient(135deg, rgba(44, 62, 80, 0.9), rgba(26, 37, 47, 0.8)), url('data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><rect fill=%22%232C3E50%22 width=%22100%22 height=%22100%22/></svg>');"></div>
                <div class="collection-overlay"></div>
                <div class="collection-content">
                    <h3>Cotton Shirts</h3>
                    <p>CLASSIC COMFORT</p>
                    <a href="#" class="collection-link">Explore Now</a>
                </div>
            </div>
            <div class="collection-card">
                <div class="collection-bg" style="background: linear-gradient(135deg, rgba(52, 73, 94, 0.9), rgba(44, 62, 80, 0.8)), url('data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><rect fill=%22%2334495E%22 width=%22100%22 height=%22100%22/></svg>');"></div>
                <div class="collection-overlay"></div>
                <div class="collection-content">
                    <h3>Cotton Pants</h3>
                    <p>TIMELESS DESIGN</p>
                    <a href="#" class="collection-link">Explore Now</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Products -->
    <section class="products">
        <div class="section-header">
            <p class="section-label">CURATED SELECTION</p>
            <h2 class="section-title">Featured Pieces</h2>
        </div>
        <div class="product-grid">
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.3)),
                    repeating-linear-gradient(45deg, #D4A574 0px, #D4A574 20px, #C09858 20px, #C09858 40px),
                    repeating-linear-gradient(-45deg, transparent 0px, transparent 10px, rgba(255,255,255,0.03) 10px, rgba(255,255,255,0.03) 20px);">
                    <div class="product-badge">NEW ARRIVAL</div>
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Premium Linen</div>
                    <div class="product-name">Beige Linen Shirt</div>
                    <div class="product-price">₹2,499</div>
                </div>
            </div>
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.3)),
                    repeating-linear-gradient(0deg, #B8956A 0px, #B8956A 25px, #9A7B4F 25px, #9A7B4F 50px),
                    repeating-linear-gradient(90deg, transparent 0px, transparent 15px, rgba(255,255,255,0.02) 15px, rgba(255,255,255,0.02) 30px);">
                    <div class="product-badge">BESTSELLER</div>
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Linen Trousers</div>
                    <div class="product-name">Tan Linen Pants</div>
                    <div class="product-price">₹2,199</div>
                </div>
            </div>
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.4)),
                    repeating-linear-gradient(90deg, #A89174 0px, #A89174 15px, #967d5f 15px, #967d5f 30px),
                    repeating-linear-gradient(0deg, transparent 0px, transparent 20px, rgba(255,255,255,0.03) 20px, rgba(255,255,255,0.03) 40px);">
                    <div class="product-badge">LIMITED</div>
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Pure Linen</div>
                    <div class="product-name">Olive Linen Shirt</div>
                    <div class="product-price">₹2,699</div>
                </div>
            </div>
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.25), rgba(0, 0, 0, 0.35)),
                    repeating-linear-gradient(45deg, #C4A57B 0px, #C4A57B 18px, #B8956A 18px, #B8956A 36px),
                    repeating-linear-gradient(-45deg, transparent 0px, transparent 12px, rgba(255,255,255,0.02) 12px, rgba(255,255,255,0.02) 24px);">
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Linen Blend</div>
                    <div class="product-name">Khaki Linen Pants</div>
                    <div class="product-price">₹1,999</div>
                </div>
            </div>
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.4)),
                    repeating-linear-gradient(0deg, #4A6FA5 0px, #4A6FA5 22px, #355682 22px, #355682 44px),
                    repeating-linear-gradient(90deg, transparent 0px, transparent 18px, rgba(255,255,255,0.03) 18px, rgba(255,255,255,0.03) 36px);">
                    <div class="product-badge">NEW</div>
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Premium Cotton</div>
                    <div class="product-name">Navy Cotton Shirt</div>
                    <div class="product-price">₹1,899</div>
                </div>
            </div>
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.25), rgba(0, 0, 0, 0.35)),
                    repeating-linear-gradient(90deg, #556B7E 0px, #556B7E 20px, #3E4F5E 20px, #3E4F5E 40px),
                    repeating-linear-gradient(0deg, transparent 0px, transparent 15px, rgba(255,255,255,0.02) 15px, rgba(255,255,255,0.02) 30px);">
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Cotton Twill</div>
                    <div class="product-name">Slate Cotton Chinos</div>
                    <div class="product-price">₹1,699</div>
                </div>
            </div>
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.3)),
                    repeating-linear-gradient(45deg, #E8D4B8 0px, #E8D4B8 16px, #D4A574 16px, #D4A574 32px),
                    repeating-linear-gradient(-45deg, transparent 0px, transparent 10px, rgba(0,0,0,0.03) 10px, rgba(0,0,0,0.03) 20px);">
                    <div class="product-badge">PREMIUM</div>
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Luxury Linen</div>
                    <div class="product-name">Ivory Linen Shirt</div>
                    <div class="product-price">₹2,899</div>
                </div>
            </div>
            <div class="product-card">
                <div class="product-image" style="background: 
                    linear-gradient(135deg, rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.3)),
                    repeating-linear-gradient(0deg, #8B7355 0px, #8B7355 24px, #7a6348 24px, #7a6348 48px),
                    repeating-linear-gradient(90deg, transparent 0px, transparent 16px, rgba(255,255,255,0.03) 16px, rgba(255,255,255,0.03) 32px);">
                    <div class="product-quick-view">
                        <button class="quick-btn" title="Add to Wishlist">♡</button>
                        <button class="quick-btn" title="Quick View">👁</button>
                        <button class="quick-btn" title="Add to Cart">🛒</button>
                    </div>
                </div>
                <div class="product-info">
                    <div class="product-category">Linen Classic</div>
                    <div class="product-name">Brown Linen Pants</div>
                    <div class="product-price">₹2,299</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Testimonials -->
    <section class="testimonials">
        <div class="section-header">
            <p class="section-label">TESTIMONIALS</p>
            <h2 class="section-title">What Our Clients Say</h2>
        </div>
        <div class="testimonial-slider">
            <div class="testimonial">
                <p class="testimonial-text">"The quality is exceptional. Every piece feels luxurious and the attention to detail is remarkable. This is truly premium menswear."</p>
                <p class="testimonial-author">— RAHUL SHARMA, MUMBAI</p>
            </div>
            <div class="testimonial-dots">
                <div class="dot active"></div>
                <div class="dot"></div>
                <div class="dot"></div>
            </div>
        </div>
    </section>
    <% if (showReferralModal) { %>
<div id="referralModal" class="ref-modal-overlay">
  <div class="ref-modal">
    <h2>Enter Referral Code</h2>
    <p>Get ₹100 instantly in your wallet!</p>

    <input id="referralInput" placeholder="Enter referral code">

    <div class="modal-buttons">
      <button id="skipReferral">Skip</button>
      <button id="applyReferral">Apply</button>
    </div>
  </div>
</div>
<% } %>

    <!-- Include Footer -->
    <div id="footer-container"></div>

    <script>
    function showToast(msg) {
  const toast = document.getElementById("toast");
  toast.innerText = msg;
  toast.classList.add("show");

  setTimeout(() => { 
    toast.classList.remove("show"); 
  }, 2500);
}
document.addEventListener("DOMContentLoaded", () => {
  const skipBtn = document.getElementById("skipReferral");
  const applyBtn = document.getElementById("applyReferral");

  if (skipBtn) {
    skipBtn.onclick = async () => {
      await fetch("/referral/skip", { method: "POST" });
      document.getElementById("referralModal").style.display = "none";
      showToast("Referral skipped");
    };
  }

  if (applyBtn) {
    applyBtn.onclick = async () => {
      const codeEl = document.getElementById("referralInput");
      const code = codeEl?.value.trim();

      if (!code) return showToast("Enter a referral code");

      const res = await fetch("/referral/apply", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ code })
      });

      const data = await res.json();

      showToast(data.message);

      if (data.success) {
        setTimeout(() => location.reload(), 800);
      }
    };
  }
});


        

        // Scroll animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        document.querySelectorAll('.collection-card, .product-card').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(50px)';
            el.style.transition = 'all 0.8s ease';
            observer.observe(el);
        });
    </script>
    <div id="toast" class="toast"></div>

</body>

</html>
   <%- include('../partials/user/footer') %> 