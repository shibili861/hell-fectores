
<style>
    /* Premium Modern UI - White & Light Brown (Old Money Elegance) Theme */
/* Apply only styling - NO HTML changes, preserves all functionality */

@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;700&family=Inter:wght@300;400;500;600&display=swap');

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', sans-serif;
  background: linear-gradient(135deg, #faf9f7 0%, #f5f2ed 100%);
  color: #3d3a35;
  min-height: 100vh;
  line-height: 1.6;
}

/* Sidebar Gap & Layout */
.container {
  margin-left: 280px; /* Adjust based on your sidebar width + gap */
  padding: 2.5rem 3rem;
  max-width: 1100px;
  margin-top: 2rem;
}

/* Typography - Old Money Elegance */
h2 {
  font-family: 'Playfair Display', serif;
  font-size: 2.4rem;
  font-weight: 600;
  color: #8b6f47;
  margin-bottom: 1.5rem;
  letter-spacing: 0.5px;
  position: relative;
  padding-bottom: 0.75rem;
}

h2::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 80px;
  height: 2px;
  background: linear-gradient(to right, #d4af37, #8b6f47);
  border-radius: 2px;
}

h4 {
  font-family: 'Playfair Display', serif;
  font-size: 1.5rem;
  color: #8b6f47;
  margin: 2rem 0 1rem;
  font-weight: 500;
  letter-spacing: 0.3px;
}

/* Card-like Container */
.container > div {
  background: #ffffff;
  border-radius: 16px;
  padding: 2.5rem;
  box-shadow: 0 8px 32px rgba(139, 111, 71, 0.08);
  border: 1px solid rgba(212, 175, 55, 0.12);
  transition: all 0.3s ease;
}

.container > div:hover {
  box-shadow: 0 12px 40px rgba(139, 111, 71, 0.12);
  transform: translateY(-2px);
}

/* Text Styling */
p {
  margin-bottom: 0.9rem;
  font-size: 1.02rem;
  color: #5c564d;
}

p strong {
  color: #8b6f47;
  font-weight: 600;
  font-size: 1.05rem;
}

/* Table - Premium Redesign */
.table {
  border-collapse: separate;
  border-spacing: 0;
  margin-top: 1.5rem;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.06);
  background: #fff;
}

.table thead {
  background: linear-gradient(135deg, #8b6f47, #a58968);
  color: #ffffff;
}

.table thead th {
  padding: 1.2rem 1rem;
  font-weight: 500;
  font-size: 0.95rem;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  border: none;
  font-family: 'Inter', sans-serif;
}

.table tbody tr {
  transition: all 0.25s ease;
  border-bottom: 1px solid #f0ede7;
}

.table tbody tr:hover {
  background-color: #fdfaf5;
  transform: scale(1.005);
  box-shadow: 0 4px 12px rgba(139, 111, 71, 0.08);
}

.table tbody td {
  padding: 1.1rem 1rem;
  vertical-align: middle;
  font-size: 0.98rem;
  color: #4a443a;
}

.table tbody td:first-child {
  font-weight: 600;
  color: #8b6f47;
}

.table tbody td:nth-child(2) {
  font-weight: 500;
}

/* Status Badge Styling */
td:contains("Pending"), td:contains("pending") {
  color: #d4af37;
  font-weight: 600;
}

td:contains("Delivered"), td:contains("delivered") {
  color: #5a8b5a;
  font-weight: 600;
}

td:contains("Cancelled"), td:contains("cancelled") {
  color: #a85a5a;
  font-weight: 600;
}

/* Summary Section */
h4.mt-3 + p {
  font-size: 1.15rem;
  margin-bottom: 0.8rem;
}

h4.mt-3 + p strong {
  font-size: 1.2rem;
  color: #8b6f47;
}

/* Address Pre Block */
pre {
  background: #f8f5f0;
  padding: 1.5rem;
  border-radius: 10px;
  border-left: 4px solid #d4af37;
  font-family: 'Courier New', monospace;
  font-size: 0.92rem;
  color: #5c564d;
  margin: 1rem 0;
  white-space: pre-wrap;
  box-shadow: inset 0 2px 8px rgba(0,0,0,0.03);
}

/* Back Button - Premium */
.btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.85rem 1.8rem;
  font-size: 1rem;
  font-weight: 500;
  text-decoration: none;
  border-radius: 12px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  margin-top: 1.5rem;
  border: none;
  cursor: pointer;
  font-family: 'Inter', sans-serif;
}

.btn-secondary {
  background: linear-gradient(135deg, #8b6f47, #a58968);
  color: white;
  box-shadow: 0 4px 15px rgba(139, 111, 71, 0.3);
}

.btn-secondary:hover {
  background: linear-gradient(135deg, #7a5f3d, #947a5a);
  transform: translateY(-3px);
  box-shadow: 0 8px 25px rgba(139, 111, 71, 0.4);
}

.btn-secondary:active {
  transform: translateY(-1px);
}

/* Responsive Adjustments */
@media (max-width: 992px) {
  .container {
    margin-left: 80px;
    padding: 2rem 1.5rem;
  }
  
  h2 {
    font-size: 2rem;
  }
  
  .table thead th,
  .table tbody td {
    padding: 0.8rem 0.6rem;
    font-size: 0.9rem;
  }
}

@media (max-width: 768px) {
  .container {
    margin-left: 0;
    padding: 1.5rem;
  }
  
  .table {
    font-size: 0.85rem;
  }
  
  .table thead {
    display: none;
  }
  
  .table tbody tr {
    display: block;
    margin-bottom: 1rem;
    border: 1px solid #e8e2d9;
    border-radius: 10px;
    padding: 1rem;
  }
  
  .table tbody td {
    display: block;
    text-align: right;
    padding: 0.5rem 0;
    border: none;
  }
  
  .table tbody td::before {
    content: attr(data-label);
    float: left;
    font-weight: 600;
    color: #8b6f47;
    text-transform: uppercase;
    font-size: 0.8rem;
    letter-spacing: 0.5px;
  }
}

/* Subtle Animations */
* {
  animation: fadeIn 0.6s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* Custom Scrollbar (Webkit) */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: #f5f2ed;
}

::-webkit-scrollbar-thumb {
  background: #d4af37;
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: #8b6f47;
}
</style>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
      <%- include('../partials/admin/sidebar') %>
</head>
<body>
    


<div class="container mt-4">
  <h2>Order Details - <%= order.orderId %></h2>
  <p><strong>Date:</strong> <%= new Date(order.createdOn).toLocaleString() %></p>

  <p><strong>User:</strong> 
    <%= order.userId ? order.userId.name : 'Guest' %> 
    (<%= order.userId ? order.userId.email : '' %>)
  </p>

  <p><strong>Payment:</strong> <%= order.paymentMethod %></p>
  <p><strong>Status:</strong> <%= order.status %></p>
  <p><strong>Discount:</strong> ₹<%= order.discount %></p>

  <h4 class="mt-4">Items</h4>

  <% if (order.orderedItems && order.orderedItems.length > 0) { %>
    <table class="table table-bordered mt-2">
      <thead class="table-dark">
        <tr>
          
          <th>Product</th>
          <th>Quantity</th>
          <th>Price</th>
          <th>Subtotal</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
  <% order.orderedItems.forEach((item, index) => { %>
    <tr>
      <td><%= index + 1 %></td>

      <td>
        <% if (item.product) { %>
          <div style="display:flex; align-items:center; gap:10px;">
            <% let imgSrc = item.product.productImage?.[0]; %>

            <% if (imgSrc) { %>
              <img src="<%= imgSrc %>" width="60" height="60"
                style="object-fit:cover; border-radius:8px;">
            <% } else { %>
              <div style="width:60px;height:60px;background:#eee;display:flex;
                          align-items:center;justify-content:center;border-radius:8px;">No Image</div>
            <% } %>

            <span><%= item.product.productName %></span>
          </div>
        <% } else { %>
          <span>Product Removed</span>
        <% } %>
      </td>

      <td><%= item.quantity %></td>
      <td>₹<%= item.price %></td>
      <td>₹<%= item.price * item.quantity %></td>

      <!--STATUS RETURN ACTIONS in the same column -->
      <td>
        <span class="status-badge status-<%= item.status.toLowerCase().replace(/\s+/g, '-') %>">
          <%= item.status %>
        </span>

        <!-- Show return details only when requested -->
        <% if (item.returnRequested && item.status === "Delivered") { %>
          <div class="alert alert-warning mt-2 p-2">
            <strong>Return Reason:</strong> <%= item.returnReason %>
          </div>

          <!-- Approve -->
          <form action="/admin/orders/<%= order.orderId %>/approve-return" method="POST" style="display:inline;">
            <input type="hidden" name="itemId" value="<%= item._id %>">
            <button class="btn btn-success btn-sm">Approve</button>
          </form>

          <!-- Reject -->
          <form action="/admin/orders/<%= order.orderId %>/reject-return" method="POST" style="display:inline;">
            <input type="hidden" name="itemId" value="<%= item._id %>">
            <button class="btn btn-danger btn-sm">Reject</button>
          </form>
        <% } %>
      </td>
    </tr>
  <% }); %>
</tbody>

    </table>
  <% } else { %>
    <p>No items found in this order.</p>
  <% } %>

  <h4 class="mt-3">Summary</h4>
  <p><strong>Total Price:</strong> ₹<%= order.totalPrice %></p>
  <p><strong>Final Amount (after discount):</strong> ₹<%= order.finalAmount %></p>

  <h4 class="mt-3">Shipping Address</h4>
  <pre><%= JSON.stringify(order.address, null, 2) %></pre>

  <a href="/admin/orders" class="btn btn-secondary mt-3">Back to Orders</a>
</div>
</body>
</html>